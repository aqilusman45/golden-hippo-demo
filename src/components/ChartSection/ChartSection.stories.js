import * as React from 'react'

import ChartSection from './ChartSection'

export default {
  title: 'Components/ChartSection',
  component: ChartSection,
}

const Template = (args) => <ChartSection {...args} />

export const Default = Template.bind({})
Default.args = {}
